import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Standard Vite config for a React app. No extra setup needed for this project.
export default defineConfig({
  plugins: [react()],
})
